<h1>THINGS TO DO:</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <h2><%= task.completed ? "[X]" : "[ ]" %> - <%= link_to task.title, task_path(task) %></h2>
    </li>
  <% end %>
</ul>
<p><%= link_to "ADD TASK", new_task_path(@task) %></p>
